# Manual commands to execute in order to complete SSH connexion

---

> 🤖 This file has been generated automatically through Ansible and **should not be edited by hand**.
> See the [original script](https://github.com/youpiwaza/ansible-install-web-server/blob/master/ansible/roles/users/tasks/generate-users-manual-commands.yml)
> & [original template file](https://github.com/youpiwaza/ansible-install-web-server/blob/master/ansible/roles/users/templates/ssh-users-manual-commands.md.j2).
> *Generated (yyyy-mm-dd) {{ currentDateTime }}*.

---

```bash
# (Once per terminal) Start the local ssh-agent
eval `ssh-agent`

# (Once per terminal) Add private key to local ssh-agent
ssh-add ~/.ssh/{{ item.name }}-ssh-key-ed25519
(pass phrase)

# (Optionnal) Verify key has been added to local ssh agent
ssh-add -l

## Try connecting with the new custom SSH port
ssh {{ item.name }}@{{ global__remote_host }} -p {{ global__custom_ssh_port }}
```
