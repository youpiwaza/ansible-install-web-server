---
### Crafts and tests
## Prevents me from commenting/uncommenting/forgetting on final production files ^^'
# > ansible-playbook -i hostsWithCustomSSHPort 99-craft-and-tests.yml

# ---
# Currently working on: Clean https management for treafik

# ---

# You'll need the_builder_guy to have it's ssh key added to the local agent, cf. the_builder_guy generated documentation
# You'll need the_docker_guy to have it's ssh key added to the local agent, cf. the_docker_guy generated documentation

# ---

### Roadmap
# - Backups & re-injection
# - 
# - 

# For security reasons, the_docker_peon home folder is chown to root.
# the_docker_guy permissions problems : no rights in the_docker_peon folder
# Only the the_builder_guy can manage folders

## the_docker_guy
- name: 'Setup networks & volumes then deploy or update WordPress stack'
  become: no
  hosts: likorneWithCustomSSHPort
  # the_docker_guy / cf. /ansible/roles/users/vars/main.yml
  remote_user: '{{ users.2.name }}'
  vars_files:
  - defaults/main.yml
  - roles/users/defaults/main.yml
  - roles/users/vars/main.yml
  - vars/main.yml
    # 🔧 project.vars_file_path
  - roles/stack-web-wordpress--generate-playbooks/vars/clients/fof/dev--sophieberberian--com/dev--sophieberberian--com---vars_secret.yml

  roles:
    - stack-web-wordpress--deploy
...